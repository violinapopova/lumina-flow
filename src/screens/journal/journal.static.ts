import { Radius } from '@theme';

/** Gradient / blur layers on animated modal sheet */
export const journalGradientLayers = {
  modalGradient: { borderRadius: Radius.xl },
  newBtnGradient: { borderRadius: 22 },
} as const;
